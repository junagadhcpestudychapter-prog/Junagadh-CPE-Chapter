[build]
  base = "frontend"
  command = "npm install --legacy-peer-deps && npm install ajv@^8.12.0 ajv-keywords@^5.0.0 --legacy-peer-deps && CI=false npx craco build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
